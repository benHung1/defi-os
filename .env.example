# Required in production. Create a project at https://dashboard.reown.com and restrict it to your deployed origins.
# /api/health returns HTTP 503 in production until this value is configured.
NUXT_PUBLIC_REOWN_PROJECT_ID=
